.u-layout
  display flex
  font-family 'PT Sans', sans-serif
  flex-direction column
  min-height 100vh

  &__header
    position fixed
    top 2rem
    width calc(100% + 2rem)
    left -1rem

  &__content
    flex 1
